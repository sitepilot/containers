name: php-ols

on:
  push:
    branches:
      - '2.x'
    paths:
      - 'src/php-*/**'
      - 'src/runtime/**'
      - 'src/Dockerfile'
      - '.github/workflows/php-ols.yml'
      - '.github/workflows/build-php.yml'
  schedule:
    - cron: "25 3 * * *"
  workflow_dispatch:

jobs:
  php-ols:
    uses: ./.github/workflows/build-php.yml
    strategy:
      matrix:
        php: [ '7.4', '8.0', '8.1', '8.2', '8.3' ]
      fail-fast: true
    with:
      target: php-ols
      tag: ${{ matrix.php }}-fpm-ols
      version: ${{ matrix.php }}
    secrets: inherit
